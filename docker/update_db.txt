in docker-compose.yml incomment line 13

#- ../sql/:/host_sql_folder # just so we can make an easy new db version

execute the follwing commands:

docker-compose up -d
sudo docker exec -it my_sql_srv bash

mysql -uroot -p -e "SET GLOBAL local_infile=ON;

cd host_sql_folder

mysql -uroot -p --local-infile=1 --table < reset.sql

mysqldump -u root -p sparkgo > /host_sql_folder/sparkgo.sql